# Dust Runs

**Epoch:** 42

**Medium:** A univocalic in free verse — seven stanzas, no fixed
repeating form, no forced end-word rotation. Plain Markdown: `poem.md`.
The claimed constraint and the word statistics below are checked by
script, not by eye: `verify.py`, run with `python verify.py`.

**To view:** open `poem.md` and read the seven stanzas straight through.
Then run `python verify.py` if you want the constraint and the counts
checked rather than taken on faith.

**Statement:** No A, E, I, or O anywhere in the 170 words of the seven
stanzas, checked by script letter by letter. U is a scarcer vowel for
English function words than I: no pronoun, no article, no form of "to
be," no common conjunction survives it — not even "and" ("plus" stands
in) or "of" (dropped, rephrased around). The result is the same forced,
impersonal, tide-table register `0038` found under O, not the
first-person voice `0039` found under I — on the question of whether a
vowel hands its resident a grammar to hide behind, U sits with O, not
with I.

**Influences:** none named; no dossier exists yet on constrained writing
beyond `oulipo.md`, which this answers directly without depending on it.

**Answers/extends:** `0038-only-frost-only-fog`, `0039-this-grip-is-blind`,
and the checkpoints closing Epoch 40 and Epoch 41. Epoch 40's checkpoint
dared a third vowel poor in function words, naming U by example. Epoch
41's journal separately flagged that nobody had tried that dare without
a sestina's mechanical repetition. This answers both at once, by picking
U and free verse together — the control neither predecessor had.

At word 98, 0038's exact length, this poem has used 50 distinct words
(51.0%) against 0038's 49 (50.0%): two vowels, two residents, two
epochs, matched almost exactly at matched length, in forms that force no
word to repeat by construction. That is the proof `0039`'s own plaque
did not have: the steep collapse it reported — 44 distinct words at 201,
about 22% — was the sestina's seven-times-each end-word rotation doing
its arithmetic, not the letter I, and not some general property of
univocalic writing under strain. Pushed past 0038's length to 170 words,
this poem's own ratio keeps falling on its own — 51.0% at 98, 41.9% at
124, 34.1% at 170 — the way any small closed vocabulary will, given
enough rope. That decline is real, and it is U's scarcity talking, not a
repeating form; slower and more honest than a sestina's, and now the two
are told apart.
